<?xml version="1.0" encoding="utf-8"?>
<ScrollView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/app_background"
    android:fillViewport="true">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:padding="16dp">

        <TextView
            android:id="@+id/txtScreenTitle"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Thêm khoản chi"
            android:textColor="@color/text_primary"
            android:textSize="22sp"
            android:textStyle="bold" />

        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="6dp"
            android:text="Nhập thông tin khoản chi để lưu vào sổ chi tiêu"
            android:textColor="@color/text_secondary"
            android:textSize="14sp" />

        <com.google.android.material.card.MaterialCardView
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="18dp"
            app:cardBackgroundColor="@android:color/white"
            app:cardCornerRadius="22dp"
            app:cardElevation="2dp">

            <LinearLayout
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:orientation="vertical"
                android:padding="16dp">

                <TextView style="@style/FormLabel" android:text="Tên khoản chi" />
                <EditText
                    android:id="@+id/edtTitle"
                    style="@style/FormInput"
                    android:hint="Ví dụ: Ăn trưa, mua sách..." />

                <TextView style="@style/FormLabel" android:layout_marginTop="12dp" android:text="Số tiền" />
                <EditText
                    android:id="@+id/edtAmount"
                    style="@style/FormInput"
                    android:hint="Nhập số tiền"
                    android:inputType="numberDecimal" />

                <TextView style="@style/FormLabel" android:layout_marginTop="12dp" android:text="Danh mục" />
                <Spinner
                    android:id="@+id/spCategory"
                    android:layout_width="match_parent"
                    android:layout_height="52dp"
                    android:background="@drawable/bg_input"
                    android:paddingStart="12dp"
                    android:paddingEnd="12dp" />

                <TextView style="@style/FormLabel" android:layout_marginTop="12dp" android:text="Ngày chi" />
                <EditText
                    android:id="@+id/edtDate"
                    style="@style/FormInput"
                    android:hint="dd/MM/yyyy"
                    android:inputType="date" />

                <TextView style="@style/FormLabel" android:layout_marginTop="12dp" android:text="Ghi chú" />
                <EditText
                    android:id="@+id/edtNote"
                    style="@style/FormInput"
                    android:gravity="top|start"
                    android:hint="Ghi chú thêm nếu có"
                    android:minHeight="92dp" />
            </LinearLayout>
        </com.google.android.material.card.MaterialCardView>

        <Button
            android:id="@+id/btnSave"
            android:layout_width="match_parent"
            android:layout_height="52dp"
            android:layout_marginTop="18dp"
            android:background="@drawable/bg_button_primary"
            android:text="Lưu khoản chi"
            android:textAllCaps="false"
            android:textColor="@android:color/white"
            android:textSize="16sp" />

        <Button
            android:id="@+id/btnDelete"
            android:layout_width="match_parent"
            android:layout_height="50dp"
            android:layout_marginTop="10dp"
            android:background="@drawable/bg_button_danger"
            android:text="Xóa khoản chi"
            android:textAllCaps="false"
            android:textColor="@android:color/white"
            android:textSize="15sp"
            android:visibility="gone" />

        <Button
            android:id="@+id/btnCancel"
            android:layout_width="match_parent"
            android:layout_height="50dp"
            android:layout_marginTop="10dp"
            android:background="@drawable/bg_button_gray"
            android:text="Hủy"
            android:textAllCaps="false"
            android:textColor="@color/text_primary"
            android:textSize="15sp" />
    </LinearLayout>
</ScrollView>
